"""Marimo notebook template + altair plot library for axi-perf data.

See ``notebook/README.md`` for the run instructions and the
"how to add a new plot" recipe. The plot functions in :mod:`.plots`
are pure (no marimo deps), so they can be imported and reused in
ad-hoc scripts, tests, or other notebooks.

Optional install:

```
pip install 'rtl-buddy-axi-profiler[notebook]'
```
"""
